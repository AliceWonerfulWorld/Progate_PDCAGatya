Issue dependencies are represented in Issue descriptions using a `## Blocked by` section with GitHub issue references (for example `- #11`).

Reason: the currently available GitHub integration does not expose a native issue dependency mutation endpoint.
